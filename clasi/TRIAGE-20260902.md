# Issue triage — 2026-09-02

Sorted for the next round (roughly two sprints). `next/` is what those
sprints plan; `high/` is important but can wait a round; `low/` is nice
to have. Eight issues were found already finished and moved to `done/`
with a dated triage note at the bottom of each saying what closed it.

## next — plan into the next two sprints (7)

| issue | why now |
|---|---|
| `concurrent-serial-writers-wedge-the-uarte-in-both-directions` | Stakeholder-designated. First cleartext `RUN:` with a payload kills the UART both ways on local builds. Hardware proof for sprint 026 ticket 003's single-producer design. |
| `cleartext-run-hangs-the-link-under-active-telemetry` | Same defect as above (confirmed 2026-09-02); closes with it. Blocks the subscribe-telemetry-then-RUN capture pattern. |
| `single-executor-for-command-dispatch` | Carried out of sprint 026 as "architectural, not urgent" before the UART-wedge proof existed. Its single-serial-producer piece is the hardware-verified fix for the two issues above; plan that as a small first ticket, the full executor inversion second. |
| `fw-1-20260829-1-wedges-on-radio-traffic-during-motion` | Still marked critical / "do not drive over radio". Almost certainly the VFP fault fixed in 026/001 (identical CFSR 0x8200), but the radio retest 026/001 asked for was never recorded. One bench session closes or re-attributes it. |
| `frozen-encoder-read-becomes-a-phantom-zero-velocity-and-the-pid-overreacts` | Measured lunge on every failed encoder read; gopiv sees 7–45 per tour. Small kernel change with a host test already sketched. |
| `no-wire-verb-reaches-rebaseposition-so-tours-cannot-zero-their-frame` | Small sequenced wire verb; makes every radio tour axis-aligned by construction. Add a sequenced ESTOP clear in the same ticket if wanted (the cleartext `RUN:clearestop` already exists). |
| `camlink-mounts-table-is-stale-for-tigez` | Ten-line tools fix that costs ten minutes of re-derivation at the start of every tigez field session. Could go out of process. |

## high — important, next round after this one

| issue | note |
|---|---|
| `first-i2c-command-can-wedge-the-program-with-no-recovery` | Unrecoverable over the air; needs a bounded I2C transaction or a protocol-fiber watchdog. Deep (CODAL I2C has no firing timeout). |
| `i2c-fault-count-climbs-on-idle-bus` | Still no baseline and no definition of what one count means. Becomes less harmful once the frozen-encoder fix lands, but the bus faulting at rest is unexplained. |
| `ensure-is-not-reentrant-two-rigs-can-be-constructed` | Latent double-construction race on the first move from two transports. Cheap fix. Sprint 026 explicitly does not cover it. |
| `rxlinebuf-64-truncates-long-inbound-radio-lines` | Silent truncation of radio lines over 64 bytes; can strip a `#<id>` so the line is dropped as stale. One-constant fix plus a host test. |
| `radio-telemetry-loss-is-wifi-interference-at-the-relay-site` | 22% loss on the fleet channels, 9% on ch 81. Durable fix is moving the channel plan above WiFi, a three-repo change. |
| `calibration-skill-emits-a-paste-able-makecode-block` | Stakeholder priority High for students, but a feature, not a functional blocker. |
| `goto-under-closed-profile-terminates-legs-early` | Measured on 67455bf, before sprint 025 rewrote braking. Re-measure on current firmware; surface `wrongWayCount_` first. |
| `gotoworld-fixed-overshoot-survives-corrected-firmware` | Same: the fixed ~50 mm overshoot predates the kinematic decel and profile-completion ending (82eda91). Re-measure before doing anything. |
| `build-checkpoint-rejects-correct-incremental-builds` | Still rejects partial recompiles (`make_deploy.py` `_check_translation_units`). Fail-safe, but forces a full rebuild on every `--robot` switch. |

## low — nice to have

| issue | note |
|---|---|
| `changing-the-radio-group-mid-run-is-unverified` | Curriculum only sets the group in `on start`; the header comment already says the mid-run path is unverified. |
| `measure-vevov-s-true-full-duty-velocity` | Feedforward gain is inherited from tovez; only matters above ~250 mm/s on vevov. |
| `three-way-contradiction-on-which-tuning-bake-the-kernel-defaults-are` | Doc consistency. Per-robot geometry is now injected at deploy, so the fleet-wide default matters less than it did. |
| `tour-corner-fixes-are-stale-cache` | Diagnostic questions all answered; the residue is stamping `ox/oy/oh` so a stale projection is detectable. Camera-at-the-ends doctrine already covers the risk. |

## done — found already finished

| issue | closed by |
|---|---|
| `moves-crawl-and-correct-instead-of-gliding-to-a-stop` | Resolved 2026-09-02, `reports/glide-to-a-stop-20260902.md` |
| `run-fiber-motion-resets-the-board-on-fw-1-20260829-1` | Sprint 026 ticket 001, commit 6907222, 0/25 resets on gopiv |
| `first-camera-scored-tour-fails-closure-gate` | Lever-arm fix 27cf24b; 10/10 inside the 50 mm gate on fixed firmware |
| `radio-group-setup-block` | Sprint 021 ticket 005, then superseded by `setupRadio` |
| `opt-in-v6-radio-always-on-v6-serial-and-student-debug-output` | `setupRadio`/`sendString`/`sendValue` and the opt-in radio link are in `src/` |
| `estop-latches-with-no-wire-clear` and `estop-latches-with-no-wire-level-clear` | `RUN:clearestop` (commit 7d9131c) |
| `pivots-over-rotate-on-corrected-firmware` | Superseded by `pivot_overrun`, per-robot slip bake, and the yaw braking gate (fc7da40) |

Note: CLASI's `list_issues` scans only the top level of `clasi/issues/`,
so issues inside `next/`, `high/` and `low/` are invisible to it until
they are moved back up or into a sprint.
