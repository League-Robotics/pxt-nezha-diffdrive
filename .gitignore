# --- Node / npm ---
node_modules/
.npm-cache/
npm-debug.log*
package-lock.json.*

# --- Local scratch: pxt-microbit target clone, node compile cache, scratch builds ---
.tmp/

# --- MakeCode / PXT build output ---
built/
pxt_modules/
.pxt/
*.hex
*.uf2
binary.asm
binary.js

# --- Python tooling ---
__pycache__/
.venv/

# --- WiFi credentials: {"ssid": ..., "password": ...}, baked into the
# scratch copy by tools/make_deploy.py's _inject_wifi_secrets(). NEVER
# committed; copy one in from a sibling checkout (nezha-upy keeps the
# same shape).
config/wifi_secrets.json

# --- macOS / editor cruft ---
.DS_Store
._*
.vscode/settings.json
*.swp

# --- Local MakeCode editor (pxt serve + ?ws=fs) droppings ---
.header.json
.simstate.json
_history
projects/
captures/

# --- pipx/setuptools build droppings under tools/rogo ---
tools/rogo/build/
tools/rogo/*.egg-info/
